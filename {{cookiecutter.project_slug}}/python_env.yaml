python: "3.10"

# Dependencies required to build packages. This field is optional.
build_dependencies:
  - pip
  - setuptools
  - wheel==0.37.1

dependencies:
  - mlflow>=2.6.0
  - typer[all]>=0.9.0
  - numpy>=1.25.2

  # dev dependencies
  # ## ci
  - nox
  - pre-commit
  - pdoc
  # ## linting
  - ruff
  - black
  - mypy
  - isort
  # ## notebook
  - notebook>=7.0.3
  - voila>=0.5.1
  - ipywidgets>=8.1.0
  # ## pytest
  - pytest>=7.4.0
  - pytest-cov>=4.1.0
  - pytest-mock>=3.11.1
